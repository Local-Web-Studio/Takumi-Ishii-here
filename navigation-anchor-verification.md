# ナビゲーション・アンカー確認メモ

## 2026-08-13 ローカル確認

- Homeヘッダーの「プロフィール」を操作すると、固定ヘッダー直下に「プロフィール」ラベルが表示される位置へスクロールした。
- プロフィールの小ラベルは旧「人物と実務の基盤」から「プロフィール」へ更新済み。
- ヘッダー右側の「プロフィール」「CONTACT」は2px拡大した共通スタイルを使用している。
- Home・Chapter 2のフッターから `TAKUMI ISHII / SELF SALES LP` を削除済み。
- HomeのCONTACTはChapter 2の `contact-label` へ、Chapter 2ヘッダーのCONTACTは同ページの `contact-label` へ向くよう更新済み。
- Chapter 2ヘッダーのCONTACTを操作すると、固定ヘッダー直下に「はじめの一歩」ラベルが見える位置へスクロールした。
- Chapter 2フッターからも `TAKUMI ISHII / SELF SALES LP` を削除済み。

## 公開版確認

GitHub Pages公開版でも、Chapter 2ヘッダーのCONTACTを操作すると「はじめの一歩」ラベルが固定ヘッダー直下に見える位置までスクロールすることを確認した。

## 主要ジャンプ導線の再確認

Homeの「やり切れると言える根拠を見る」は `#proof-hero-label` を参照するよう更新済み。ブラウザでは初回操作によりCTAを表示位置へ移動させたため、遷移先の最終確認を続行する。

補完処理の導入後も、ブラウザの初回操作はCTAを表示位置へ移動させる挙動となった。次の操作で実際のページ間遷移を検証する。

2回目の操作では、Homeの根拠CTAからChapter 2の `#proof-hero-label` へ遷移し、「自信の根拠」ラベルが固定ヘッダー直下に見える位置へ着地した。続けて「最初から読み返す」導線を操作した。

「最初から読み返す」はHomeの `#home-title` に戻り、主見出しが固定ヘッダー直下に見える位置へ着地した。HomeのCONTACTもChapter 2の `#contact-label` に遷移し、「はじめの一歩」ラベルが固定ヘッダー直下に見える位置へ着地した。

直接URLの `#contact-label` は安定化後に正しく最終CVラベルへ着地した。一方で、同一ページ内で遷移先を `#proof-hero-label` に切り替えた直接URLは中間位置に留まったため、ハッシュ変更時のタイミングを追加で確認する。

遅延再補正を加えた後、直接URLの `#proof-hero-label` は「自信の根拠」ラベルが固定ヘッダー直下に見える位置へ正しく着地した。

## 固定ヘッダー3導線の確認

Chapter 2の固定ヘッダーはTOP・PROFILE・CONTACTの3導線へ統一済み。Chapter 2のPROFILEを操作すると、Homeの「プロフィール」ラベルが固定ヘッダー直下に見える位置へ着地した。
